<div class="container">
  <% if user_signed_in? %>
    <% if current_user.admin? %>
      <div class="article-index">
        <h3>Administration</h3>
        <div class="admin-buttons">
          <ul>
            <li><button class="btn-back-white"><%= link_to "Nouvel article", new_article_path %></button></li>
            <li><button class="btn-back-white"><%= link_to "Nouvelle lecture", new_book_path %></button></li>
          </ul>
          <ul>
            <li><button class="btn-back-white"><%= link_to "Gérer les commentaires", comments_path %></button></li>
          </ul>
        </div>
      </div>
      <% else %>
        <p>Vous n'êtes pas un administrateur du site. Merci de retourner à l'accueil.</p>
        <%= button_to "Retour à l'accueil", root_path, class:"btn btn-back-grey", method: "get" %>
      <% end %>
  <% else %>
    <p>Si vous êtes administrateur, vous pouvez vous connecter <%= link_to "en cliquant ici", new_user_session_path, target: "_blank" %>.</p>
    <p>Si vous êtes un lecteur, veuillez retourner à l'accueil.</p>
    <%= button_to "Retour à l'accueil", root_path, class:"btn btn-back-grey", method: "get" %>
  <% end %>

</div>
